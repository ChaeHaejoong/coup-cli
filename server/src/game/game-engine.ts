import Player from "./entity/player";

export class GameEngine {
  private deck = Array(15);
  private playerList;
  
  constructor(playerList: Player[]) {
    this.playerList = playerList;
  }
  
  public init() {
    
  }
  
  private setDeck() {
    
  }
  
  private shuffleDeck() {
    
  }
} 